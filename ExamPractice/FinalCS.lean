namespace hidden -- Namespace hidden allows us do define our own inductive 
                    --types even if they have a standard parallel
/-
To calculate the number of functions that can be made
    x = # of inputs, y = # of output 
    # of functions = (2^y)^(2^x)  assuming it is a binary function
-/





end hidden